{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Email de reinitialisation envoyé{% endblock %}

{% block body %}

    <div class="content mt-5">
        <div class="columns">
            <div class="column is-half is-offset-one-quarter">
                <p>
                    Si un compte correspond a l'email renseigné, alors un mail viens de vous être transmis pour reinitialiser votre mot de passe.
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>

                <p>Si vous n'avez pas reçu d'email, vérifiez dans votre dossier de courriers indésirables ou <a href="{{ path('app_forgot_password_request') }}">réessayez</a></p>
            </div>
        </div>
    </div>
    <div class="content">

    </div>
{% endblock %}
